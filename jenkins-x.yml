buildPack: charts 
pipelineConfig:
  pipelines:
    release:
      promote:
        steps:
        - command: jx step changelog
        - command: jx step create pr chart --name zeebe-operate --version $VERSION  --repo http://github.com/zeebe-io/zeebe-full-helm.git 
